{
  "name": "hs-plugin-strava",
  "version": "1.0.0",
  "description": "Strava plugin for Home Screens",
  "license": "MIT",
  "scripts": {
    "dev": "vite build && (vite build --watch & npx -y serve@latest . -l 5173 --cors --no-clipboard)",
    "build": "vite build",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "audit:build": "vite build --config vite.audit.config.ts"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "jsdom": "^29.1.1",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^4.1.9"
  }
}
